<%= form_for(current_user.student_clubs.build(club_id: @club.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :club_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>